# Use your machine's LAN IP instead of localhost (e.g. http://192.168.1.23:4000/api) to
# access the app from other devices on the network — "localhost" only resolves to the
# device the browser is running on, not this machine.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
